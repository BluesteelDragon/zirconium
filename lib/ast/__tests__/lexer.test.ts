/* eslint-disable ts/no-magic-numbers -- test file */
import { describe, expect, it } from "@rbxts/jest-globals";

describe("zirconium lexer", () => {
	it("should lex", () => {
		expect(6 / 2).toBe(3);
	});
});

export = true;
